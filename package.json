{
  "name": "section508dash",
  "version": "1.0.0",
  "description": "Web services to see 508 compliance predictions for solicitations posted to FBO",
  "main": "index.js",
  "scripts": {
    "start": "node_modules/.bin/sequelize db:migrate && node server/server.js",
    "dev": "export NODE_ENV=development && export MAIL_ENGINE=nodemailer-mock && node_modules/.bin/sequelize db:migrate && node server/server.js",
    "old-test": "export NODE_ENV=test || SET NODE_ENV=test && mocha server/**/*.test.js",
    "test": "MAIL_ENGINE=nodemailer-mock  jest --detectOpenHandles --config=jest.config.js --runInBand ",
    "test-watch": "export NODE_ENV=development && export MAIL_ENGINE=nodemailer-mock &&  jest --detectOpenHandles --config=jest.config.js --watch ",
    "backup-db": "export PGPASSWORD=***REMOVED*** && pg_dump -U utz6kah1zq8t2nyg -d cgawsbrokerprodgq3wrvn8c8a772q -h 192.168.1.126 > srt-database.sql ",
    "restore-db": "export PGPASSWORD=***REMOVED*** && export PGUSER=utz6kah1zq8t2nyg && export PGHOST=192.168.33.101 && export PGDATABASE=cgawsbrokerprodgq3wrvn8c8a772q &&  rm -f srt-database.sql* srt-database.sql.gz && cp srt-database-export.sql.gz srt-database.sql.gz && gzip -d srt-database.sql.gz && dropdb  ${PGDATABASE} && createdb ${PGDATABASE} && psql  <srt-database.sql ",
    "doc": "node_modules/.bin/jsdoc  -d docs -R ./README.md -r server/routes server/models"
  },
  "author": "TCG",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.14.0",
    "express-winston": "^3.0.1",
    "jsonwebtoken": "^7.3.0",
    "lodash": "^4.17.4",
    "multer": "^1.3.0",
    "nodemailer": "^5.1.1",
    "pg": "^7.8.0",
    "sequelize": "^4.42.0",
    "umzug": "^2.2.0",
    "winston": "^3.2.0",
    "random-words": "^1.1.0",
    "randomstring": "*",
    "sequelize-auto": "^0.4.29",
    "sequelize-cli": "^5.4.0"
  },
  "devDependencies": {
    "docdash": "^1.0.3",
    "expect": "^1.20.2",
    "jest": "~23",
    "jsdoc": "^3.5.5",
    "mocha": "^3.2.0",
    "mock-express-request": "^0.2.2",
    "mock-express-response": "^0.2.2",
    "nodemailer-mock": "^1.3.15",
    "sqlite3": "^4.0.6",
    "supertest": "^2.0.1"
  },
  "engines": {
    "node": "8.15",
    "npm": "3.10.3"
  }
}
