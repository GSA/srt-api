image: node:8


stages:
  # - build
  - test

cache:
  untracked: true
  key: "$CI_COMMIT_REF_NAME"
  paths:
    - node_modules/

#install_dependencies:
#  stage: build
#  script:
#    - npm install
#  artifacts:
#    paths:
#      - node_modules/

test_with_lab:
  stage: test
  coverage: '/All files[^0-9]*\d+\.\d+/'
  artifacts:
    paths:
      - coverage/
  script:
    - npm install
    - export NODE_ENV=sqlite
    - npm test